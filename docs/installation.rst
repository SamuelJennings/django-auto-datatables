============
Installation
============

At the command line::

    $ easy_install django-auto-datatables

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-auto-datatables
    $ pip install django-auto-datatables
